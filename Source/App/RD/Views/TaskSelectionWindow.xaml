<Window x:Class="RD.Views.TaskSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        mc:Ignorable="d"
        Title="Add URL to Task" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ui:WindowHelper.UseModernWindowStyle="True">
    
    <Window.Resources>
        <!-- Mica-like background -->
        <LinearGradientBrush x:Key="MicaBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#F3F3F3" Offset="0"/>
            <GradientStop Color="#F0F0F0" Offset="0.5"/>
            <GradientStop Color="#EEEEEE" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    
    <Grid Background="{StaticResource MicaBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource AccentColorBrush}" 
                Padding="24,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="?? " FontSize="24" Margin="0,0,12,0"/>
                <TextBlock Text="Add URL to Scheduled Task" 
                           FontSize="20" 
                           FontWeight="SemiBold" 
                           Foreground="White"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- URL Display -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="24,16,24,8">
            <StackPanel>
                <TextBlock Text="URL to Add:" 
                           FontWeight="SemiBold" 
                           FontSize="14" 
                           Margin="0,0,0,8"/>
                <TextBlock Text="{Binding Url}" 
                          TextWrapping="Wrap" 
                          Background="{StaticResource SystemControlBackgroundBaseLowBrush}"
                          Padding="12"
                          FontFamily="Consolas"/>
            </StackPanel>
        </Border>
        
        <!-- Tasks List -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="24,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Text="Select a Task:" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,12"
                           Foreground="{StaticResource AccentColorBrush}"/>

                <DataGrid Grid.Row="1"
                         ItemsSource="{Binding Tasks}"
                         SelectedItem="{Binding SelectedTask}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         IsReadOnly="True"
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column"
                         SelectionMode="Single"
                         AlternatingRowBackground="#F8F8F8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Task Name" Binding="{Binding Name}" Width="200"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="140"/>
                        <DataGridTextColumn Header="Downloads" Binding="{Binding Downloads.Count}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
        
        <!-- File Path Selection -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="24,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="Download Location:" 
                           FontWeight="SemiBold" 
                           FontSize="14" 
                           Margin="0,0,0,8"/>

                <TextBox Grid.Row="1" Grid.Column="0"
                         Text="{Binding FilePath}" 
                         Style="{StaticResource FluentTextBoxStyle}"
                         Margin="0,0,8,0"/>
                         
                <Button Grid.Row="1" Grid.Column="1"
                        Content="Browse..." 
                        Command="{Binding BrowseCommand}" 
                        Style="{StaticResource FluentSecondaryButtonStyle}"/>
            </Grid>
        </Border>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="24,8,24,24">
            <Button Content="Add to Task" 
                    Command="{Binding AddToTaskCommand}" 
                    Style="{StaticResource FluentButtonStyle}" 
                    IsDefault="True"/>
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}" 
                    Style="{StaticResource FluentSecondaryButtonStyle}" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>