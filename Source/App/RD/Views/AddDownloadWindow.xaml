<Window x:Class="RD.Views.AddDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        mc:Ignorable="d"
        Title="Add New Download" Height="480" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ui:WindowHelper.UseModernWindowStyle="True">
    
    <Window.Resources>
        <!-- Mica-like background -->
        <LinearGradientBrush x:Key="MicaBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#F3F3F3" Offset="0"/>
            <GradientStop Color="#F0F0F0" Offset="0.5"/>
            <GradientStop Color="#EEEEEE" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid  Background="{StaticResource MicaBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24 12">
                <!-- URL Section -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Download URL" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,16"
                               Foreground="{StaticResource AccentColorBrush}"/>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                   Text="??" 
                                   FontSize="16" 
                                   VerticalAlignment="Center" 
                                   Margin="0,0,12,0"/>
                            <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2">
                                <Label Content="URL:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding Url}" 
                                     Style="{StaticResource FluentTextBoxStyle}"
                                     Margin="0,0,0,12"/>
                            </StackPanel>

                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                   Text="??" 
                                   FontSize="16" 
                                   VerticalAlignment="Center" 
                                   Margin="0,0,12,0"/>
                            <StackPanel Grid.Row="1" Grid.Column="1">
                                <Label Content="File Name:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding FileName}" 
                                     Style="{StaticResource FluentTextBoxStyle}"
                                     Margin="0,0,8,12"/>
                            </StackPanel>

                            <TextBlock Grid.Row="2" Grid.Column="0" 
                                   Text="??" 
                                   FontSize="16" 
                                   VerticalAlignment="Center" 
                                   Margin="0,0,12,0"/>
                            <StackPanel Grid.Row="2" Grid.Column="1">
                                <Label Content="Save to:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding DownloadPath}" 
                                     Style="{StaticResource FluentTextBoxStyle}"
                                     Margin="0,0,8,0"/>
                            </StackPanel>
                            <Button Grid.Row="2" Grid.Column="2" 
                                Content="Browse..." 
                                Style="{StaticResource FluentSecondaryButtonStyle}"
                                Command="{Binding BrowseDownloadPathCommand}"
                                VerticalAlignment="Bottom"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Download Options -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Download Options" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,16"
                               Foreground="{StaticResource AccentColorBrush}"/>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,12">
                                <Label Content="Max Segments:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding MaxSegments}" Style="{StaticResource FluentTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,12">
                                <Label Content="Buffer Size:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding BufferSize}" Style="{StaticResource FluentTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,12">
                                <Label Content="Timeout (minutes):" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding TimeoutMinutes}" Style="{StaticResource FluentTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,12">
                                <Label Content="Retry Attempts:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding RetryAttempts}" Style="{StaticResource FluentTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,12">
                                <Label Content="Retry Delay (seconds):" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                <TextBox Text="{Binding RetryDelaySeconds}" Style="{StaticResource FluentTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                                   Orientation="Horizontal" 
                                   Margin="0,8,0,0">
                                <CheckBox Content="Overwrite existing file" 
                                      IsChecked="{Binding OverwriteExisting}" 
                                      Margin="0,0,24,0"/>
                                <!--<CheckBox Content="Enable resume" 
                                      IsChecked="{Binding EnableResume}"/>-->
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Proxy Settings -->
                <Expander Header="Proxy Settings" Margin="0,0,0,16">
                    <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                        <StackPanel>
                            <CheckBox Content="Use Proxy" 
                                 IsChecked="{Binding UseProxy}" 
                                 Margin="0,0,0,16"/>

                            <Grid IsEnabled="{Binding UseProxy}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,12">
                                    <Label Content="Host:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <TextBox Text="{Binding ProxyHost}" Style="{StaticResource FluentTextBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,12">
                                    <Label Content="Port:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <TextBox Text="{Binding ProxyPort}" Style="{StaticResource FluentTextBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
                                    <Label Content="Username:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <TextBox Text="{Binding ProxyUsername}" Style="{StaticResource FluentTextBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,0">
                                    <Label Content="Password:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <PasswordBox x:Name="ProxyPasswordBox" 
                                           Height="36" 
                                           Margin="4"
                                           Padding="12,8"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Expander>

                <!-- Authentication Settings -->
                <Expander Header="Authentication" Margin="0,0,0,24">
                    <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                        <StackPanel>
                            <CheckBox Content="Use Authentication" 
                                 IsChecked="{Binding UseAuthentication}" 
                                 Margin="0,0,0,16"/>

                            <Grid IsEnabled="{Binding UseAuthentication}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                    <Label Content="Type:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <ComboBox ItemsSource="{Binding AuthenticationTypes}" 
                                         SelectedItem="{Binding AuthenticationType}" 
                                         Height="36"
                                         Margin="4"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Margin="0,0,0,12">
                                    <Label Content="Username:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <TextBox Text="{Binding AuthUsername}" Style="{StaticResource FluentTextBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Margin="0,0,0,12">
                                    <Label Content="Password:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <PasswordBox x:Name="AuthPasswordBox" 
                                           Height="36" 
                                           Margin="4"
                                           Padding="12,8"/>
                                </StackPanel>

                                <StackPanel Grid.Row="3">
                                    <Label Content="Token/API Key:" FontWeight="SemiBold" Padding="0,0,0,4"/>
                                    <TextBox Text="{Binding AuthToken}" Style="{StaticResource FluentTextBoxStyle}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Expander>

            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}"
                Padding="10">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add Download" 
                    Command="{Binding AddDownloadCommand}" 
                    Style="{StaticResource FluentButtonStyle}"/>
                <Button Content="Cancel" 
                    Command="{Binding CancelCommand}" 
                    Click="CancelButton_Click" 
                    Style="{StaticResource FluentSecondaryButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>